<h1>Subs on Subs on Subs</h1>
<h5>It's our way</h5>

<ul>
  <% @subs.each do |sub_instance| %>
  <li>
    <%= link_to sub_instance.title, sub_url(sub_instance) %>
  </li>
  <br>
<% end %>
</ul>

<br>
<%= link_to "Add a sub", new_sub_url %>
